spring.application.name=SupermercadoPruebaTecnica

# ==========================
# Configuraciones de JPA + MySQL
# ==========================
spring.datasource.url=${DB_URL}
spring.datasource.username=${DB_USER_NAME}
spring.datasource.password=${DB_PASSWORD}
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=${DB_PLATFORM}
server.port=8081
# # ========================
# # DATASOURCE (H2)
# ========================
# spring.datasource.url=jdbc:h2:mem:superdb;MODE=MySQL;DB_CLOSE_DELAY=-1
# spring.datasource.driverClassName=org.h2.Driver
# spring.datasource.username=dasafodata
# spring.datasource.password=
#
# # ========================
# # JPA / HIBERNATE
# # ========================
# spring.jpa.hibernate.ddl-auto=update
# spring.jpa.show-sql=true
# spring.jpa.properties.hibernate.format_sql=true
#
# # Para que H2 se porte como MySQL (importante para tests)
# spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
#
# # ========================
# # H2 CONSOLE
# # ========================
# spring.h2.console.enabled=true
# spring.h2.console.path=/h2-console


# -- Crear usuario que puede conectarse desde localhost
# CREATE USER 'tu_usuario'@'localhost' IDENTIFIED BY 'tu_password';
#
# -- Darle permisos totales (solo para entornos locales de desarrollo)
# GRANT ALL PRIVILEGES ON *.* TO 'tu_usuario'@'localhost';
#
# -- Aplicar cambios
# FLUSH PRIVILEGES;